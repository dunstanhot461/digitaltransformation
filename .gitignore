# Secrets / local config — never commit
.env
.env.local
# firebase-config.js — committed because Firebase web apiKey is non-secret;
# protection is via Database Rules + Authorized Domains + App Check.

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
*.log

# Playwright MCP artifacts
.playwright-mcp/
